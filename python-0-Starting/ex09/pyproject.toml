[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A sample test package"
authors = [
    { name = "yel-alja", email = "yel-alja@student.1337.ma" }
]
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"

[tool.setuptools.packages.find]
where = ["."]